import React from 'react';
import styled from 'styled-components';

const MediaPosting = () => {
  return (
    <MediaPostingWrapper>
      <div>MediaPosting</div>
    </MediaPostingWrapper>
  );
};

export default MediaPosting;

const MediaPostingWrapper = styled.div``;
